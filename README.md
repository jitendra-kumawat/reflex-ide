# Reflex IDE

## Introduction

It is a integrated development environment (IDE).

## Running the application

- Install dependencies.
    ``` bash
    npm install
    ```
- Run node server.
    ``` bash
    npm start
    ```